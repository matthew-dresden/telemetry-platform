# terragrunt/env.hcl
#
# Repo-wide non-secret defaults.
# This file is optional (spec 02 Section 1.1); it carries no secrets and no account IDs.
# All environment-specific values are sourced from the per-account account.hcl layer (D1).

locals {}
